# Root-level env example
# Copy to .env at workspace root if needed for shared tooling.
# Each app has its own .env.example under apps/backend/ and apps/frontend/.

# Node environment
NODE_ENV=development
